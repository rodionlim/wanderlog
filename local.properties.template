# Copy this file to local.properties and fill in your values.
# local.properties is git-ignored.

sdk.dir=/path/to/android/sdk

# These keys are optional here — the app stores them securely via Settings screen.
# If you want to bake the Maps key into the manifest for development, set it here
# and reference via manifestPlaceholders in app/build.gradle.kts.
MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY
