# 2026-04

## 2026-04-29

- Added a local knowledge base under `docs/knowledge-base/` with a topic-based index and separate topic files.
- Expanded the knowledge base with dedicated pages for map integration, settings/onboarding, and AI Generate behavior.
- Moved Trip Sync documentation ownership into a dedicated knowledge-base topic and left `docs/TripSync.md` as a compatibility pointer. See [Trip Sync](../trip-sync.md).
- Documented that AI Generate `Update days` sends existing trip-day context and appends only new items on commit. See [AI Generate](../ai-generate.md).
- Added approximate context, input, and total prompt token estimates to AI Generate `Update days`, plus overlap-aware preview defaults and per-item acceptance before commit. See [AI Generate](../ai-generate.md).
- Added approximate context, input, and total prompt token estimates to Ask About Trip. See [AI and Imports](../ai-and-imports.md).
- Refined the map screen with a centered resolving overlay, day-colored markers and routes, compact day legend chips, and bounds fitting for visible points. See [Map Integration](../map-integration.md).
- Changed flight map placement so normal trip days prefer arrival-side airports while the final trip day prefers the departure side, and older saved flight places are re-resolved when stale. See [Map Integration](../map-integration.md).
- Added a GitHub Actions pipeline badge to the README.
- Added knowledge-base maintenance guidance and split chronology into indexed monthly notes.

## 2026-04-28

- Added a first-run onboarding prompt for AI setup that appears only when no OpenAI API key is stored. See [Settings and Onboarding](../settings-and-onboarding.md).
- Added an OpenAI API key help icon and help dialog in Settings. See [Settings and Onboarding](../settings-and-onboarding.md).
- Added a Google Maps convenience action on itinerary items with an address, then refined it to prefer the item address and use a subtler in-card design. See [Map Integration](../map-integration.md).
- Improved itinerary day ordering so the active accommodation section stays on top while regular day items sort by start time.
- Added separate multi-day car-rental return entries and kept pickup/drop-off parsing distinct. See [AI and Imports](../ai-and-imports.md).
- Refreshed `README.md` and `CLAUDE.md` so they match the current import, budget, settings, and schema behavior.
